{
  "name": "profile-intelligence",
  "version": "1.0.0",
  "description": "Profile Intelligence Service — enriches names using Genderize, Agify, and Nationalize APIs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "migrate": "node -e \"require('dotenv').config(); const {runMigrations} = require('./src/db/migrate'); runMigrations().then(() => process.exit(0)).catch(e => { console.error(e); process.exit(1); })\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  }
}
